# One export command per line (new hyphenated style only).
# Lines beginning with # are ignored.

# -------------------------
# Embedding Models (fe)
# -------------------------
flouds-export export --model-name BAAI/bge-base-en-v1.5 --model-for fe --normalize-embeddings
flouds-export export --model-name sentence-transformers/all-MiniLM-L6-v2 --model-for fe --normalize-embeddings

# -------------------------
# Rankers
# -------------------------
flouds-export export --model-name cross-encoder/ms-marco-MiniLM-L-12-v2 --model-for ranker --optimize
